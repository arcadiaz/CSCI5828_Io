doctype html
html
    head
        title IO chat
        link(rel='stylesheet', type='text/css', href='css/styles.css')
        link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css')
        script(src='https://code.jquery.com/jquery-3.2.1.slim.min.js')
        script(src='https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js')
        script(src='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js')

    script(src='/socket.io/socket.io.js')
    script(src='https://code.jquery.com/jquery-1.11.1.js')
    script.
        $(function () {
            var socket = io();
            $('#chatbox').submit(function () {
                socket.emit('chat message', $('#m').val());
                $('#m').val('');
                return false;
            });
            socket.on('display messages', function (msg) {
                $('#messages').append($('<li>').text(msg));
                var objDiv = document.getElementById("messages");
                objDiv.scrollTop = objDiv.scrollHeight;

                
            });

            let channel = document.cookie.replace(/(?:(?:^|.*;\s*)channel\s*\=\s*([^;]*).*$)|^.*$/, "$1");
            let username = document.cookie.replace(/(?:(?:^|.*;\s*)username\s*\=\s*([^;]*).*$)|^.*$/, "$1");
            function inter(){
                $.getJSON('http://localhost:5000/getStats/' + username + '/' + channel,
                    function (d) {
                        $('#emote').html(d['most_frequent_emote']);
                        $('#phrase').html(d['most_frequent_phrase']);
                        $('#summary').html(d['text_summary']);
                        $('#top5').html(JSON.stringify(d['top_5_emotes']));
                        }
            )};
            setInterval(inter, 1000);
        });
    body
        h1  Io chat
        div.row
            #chatdiv.col-md-6
                ul#messages
                form#chatbox(action='/')
                    #messageBox.row
                        input#m(autocomplete='off').col-md-10
                        button.col-md-2 Send

            #statsdiv.col-md-6
                div.row
                    div.card.col-md-6
                        div.card.body
                            h5.card-title Most Frequent Emote
                            div#emote blah blah blah
                    div.card.col-md-6
                        div.card.body
                            h5.card-title Most Frequent Phrase 
                            div#phrase blah blah blah 
                div.row
                    div.card.col-md-6
                        div.card.body
                            h5.card-title Top 5 Emotes
                            div#top5 blah blah blah
                    div.card.col-md-6
                        div.card.body
                            h5.card-title Summary
                            div#summary blah blah blah 
 



